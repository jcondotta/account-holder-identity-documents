micronaut:
  server:
    port: 8174

  object-storage:
    aws:
      account-holder-identity-document:
        bucket: 'jcondotta-test-account-holder-identity-document'

aws:
  access-key-id: 'test'
  secret-key: 'test'
  region: 'us-east-1'
  services:
    s3:
      endpoint-override: 'http://127.0.0.1:4566'