micronaut:
  server:
    port: 8174

  object-storage:
    aws:
      account-holder-identity-documents:
        bucket: 'jcondotta-account-holder-identity-documents-test'

aws:
  access-key-id: 'test'
  secret-key: 'test'
  region: 'us-east-1'
  services:
    s3:
      endpoint-override: ${AWS_S3_ENDPOINT}